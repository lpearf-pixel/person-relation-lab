# 示例任务：关系规则代码审查

请审查命令中明确提供的关系规则源文件和测试文件，重点检查：

- 缺失地址是否保持中性；
- 单一单位或单一地址证据是否被错误提升为伴侣候选；
- 是否存在缺少测试的边界条件。

只返回简短结论、建议的 unified diff 和应运行的验证命令。不要声称已经执行测试，不要请求数据库、真实人员数据、日志、`.env` 或导入文件。

如果当前生产逻辑已经满足上述规则，只补缺失的回归测试，不要为了产生补丁而修改生产代码。输出前逐条推演新增测试在建议补丁上的结果，结论与补丁行为不一致时必须自行修正。
