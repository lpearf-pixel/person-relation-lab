# 开发进度

## 2026-07-19 重建检查点

- [x] TypeScript/Node工程骨架和严格门禁。
- [x] 目录递归扫描、稳定期和边界保护。
- [x] `.xlsx` 流式读取与来源行封装。
- [x] 工作表级断点续传和批处理接口。
- [x] 身份证、手机号归一规则。
- [x] 地址缺失中性与关系推断安全规则。
- [x] PostgreSQL七层初始模式。
- [x] Fastify健康、导入状态和关系查询API合同。
- [x] Docker Compose和GitHub Actions基础。
- [x] PostgreSQL批量来源行写入、检查点仓储和有界递归路径SQL。
- [x] 流式文件哈希、稳定状态转换与自动任务队列核心。
- [x] 后台扫描、SHA-256登记、自动导入、完成/隔离审计全链路。
- [x] CSV流式适配器。
- [x] 本地Web操作台与PostgreSQL关系路径查询接入。
- [x] 原始记录到人员实体、标识符、关系证据和关系投影的自动流水线。
- [x] 大文件合成与流式吞吐/峰值内存基准工具；小规模冒烟通过。
- [ ] 在目标本机执行约300MB工作簿与完整PostgreSQL压力测试。
- [x] GitHub远端发布；当前提交待CI复核。

说明：前一检查点因工作区自动维护被清除且未保存到持久区，本检查点已重建并将单独归档。

## 2026-07-22 大规模关系计算恢复

- [x] 修复新人物与 observation 同语句不可见导致的投影缺失。
- [x] 非法零年份生日安全归一为空值。
- [x] 新增 `people`、`mobile`、`address`、`company` 四阶段数据库断点。
- [x] 每个批次的数据写入、关系更新和断点推进保持原子性。
- [x] 使用来源级 advisory lock 防止并发重复恢复。
- [x] 关系生成按手机号、地址、单位哈希分别使用复合索引等值连接。
- [x] 新算法使用 `relation-v3`，历史 `relation-v2` 暂时保留。
- [x] 路径查询对同一关系优先使用 `relation-v3`，避免新旧边重复。
- [x] 串行恢复入口覆盖所有已登记但投影不一致的来源。
- [x] 暂停、恢复、监控脚本统一向 `/tmp` 输出日志。
- [ ] 在目标本机运行迁移并完成 1,000 万条现有数据的断点恢复。
- [ ] 对比恢复前后的 PostgreSQL 临时写入量和单批耗时。

## 2026-08-13 旧人物断点自愈

- [x] 复现 `raw > projected` 但四阶段均为 `complete` 的历史断点污染。
- [x] 在来源 advisory lock 内校验人物观察覆盖率。
- [x] 污染断点只失效一次；中断后保留新的已提交游标。
- [x] 人物恢复只分页读取缺少 `person_observation` 的原始记录。
- [x] 手机、地址、单位关系阶段安全归零并通过 `relation-v3` 幂等重放。
- [x] 保留全部原始记录、人物、已有证据、关系和 Docker 数据卷。
